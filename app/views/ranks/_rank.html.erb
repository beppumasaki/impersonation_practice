<tr>
  <td class='align-middle'><%= rank.score %>点</td>
  <td class='align-middle td-pe-none'><%= Target.find(rank.target_id).name %></td>
  <td class='align-middle'><%= User.find(rank.user_id).name %></td>
  <td><%=audio_tag("#{rank.impersonation_voice.url}", controls: true) %></td>
</tr>